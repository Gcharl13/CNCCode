services:
  cnc:
    build: .
    image: cnc-job-planner
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
      - DATA_DIR=/data
    volumes:
      # Job store persists on the host and survives container rebuilds.
      - ./data:/data
    restart: unless-stopped
