{
  "name": "cnc-job-planner",
  "version": "0.1.0",
  "description": "Dockerized CNC job planner / production queue. Serves the CNC Nest app and persists jobs (load DXF -> nest -> template -> NK105 G-code).",
  "main": "server/index.js",
  "scripts": {
    "start": "node server/index.js",
    "dev": "node --watch server/index.js",
    "test": "node --test test/*.test.js",
    "test:e2e": "node test/e2e.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "puppeteer": "^25.2.0"
  }
}
